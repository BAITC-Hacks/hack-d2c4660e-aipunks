{
  "name": "event-match-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Server-side event contractor assistant and deterministic matching",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test lib/tests/*.test.js",
    "serve": "npm run build && node ../backend/node_modules/firebase-tools/lib/bin/firebase.js emulators:start --config ../firebase.json --project demo-event-match --only auth,firestore,functions",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  }
}
