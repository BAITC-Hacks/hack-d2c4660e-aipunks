{
  "name": "event-match-backend",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "firebase emulators:exec --project demo-event-match --only auth,firestore \"node --test test/*.test.mjs\"",
    "emulators": "firebase emulators:start --project demo-event-match --only auth,firestore",
    "admin": "node scripts/admin.mjs"
  },
  "dependencies": {
    "firebase-admin": "^14.4.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.6.0",
    "firebase-tools": "^15.30.2"
  }
}
